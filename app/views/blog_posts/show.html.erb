<%= link_to "Edit", edit_blog_post_path(@blog_post) if user_signed_in? %>
<%= link_to "Delete", @blog_post, {turbo_method: :delete, turbo_confirm: "Are u Sure"} if user_signed_in? %>

<h1><%= @blog_post.title %></h1>

<p><%= @blog_post.body %></p>